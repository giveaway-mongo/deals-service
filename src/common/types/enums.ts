export enum ContactMethod {
  PHONE = 'phone&chat',
  CHAT = 'chat',
}

export enum DealType {
  AUCTION = 'auction',
  FIRST_BIDDER = 'firstBid',
  RANDOM = 'random',
}

export enum Status {
  PENDING = 'pending',
  REJECTED = 'rejected',
  ACTIVE = 'active',
  CLAIMED = 'claimed',
  GIVEN_AWAY = 'givenAway',
}
